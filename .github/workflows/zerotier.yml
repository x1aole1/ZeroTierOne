name: 编译zerotier

on:
  schedule:
    - cron: '0 3,20 * * *'
  workflow_dispatch:

permissions:
  contents: write
  actions: write

env:
  TZ: Asia/Shanghai
jobs:
 check:
  runs-on: ubuntu-latest
  outputs:
    ver: ${{ steps.getversion.outputs.ver }}
    should_build: ${{ steps.getversion.outputs.should_build }}
  steps:
  - name: 检查更新
    id: getversion
    shell: bash
    run: |
      set -euo pipefail
      ver="$(curl -fsSL --retry 3 --retry-delay 2 https://api.github.com/repos/zerotier/ZeroTierOne/releases/latest | jq -r '.tag_name')"
      if [[ ! "$ver" =~ ^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z]+)?$ ]]; then
        echo "Unexpected ZeroTier release tag: $ver" >&2
        exit 1
      fi
      if gh release view "$ver" --repo "${{ github.repository }}" >/dev/null 2>&1; then
        should_build=false
        echo "ZeroTier $ver 已构建并发布，跳过"
      else
        should_build=true
        echo "ZeroTier $ver 尚未发布，开始构建"
      fi
      echo "ver=${ver}" >> "$GITHUB_OUTPUT"
      echo "should_build=${should_build}" >> "$GITHUB_OUTPUT"
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  - name: delete-workflow
    uses: Mattraks/delete-workflow-runs@main
    with:
      token: ${{ secrets.GITHUB_TOKEN }}
      retain_days: 0
      keep_minimum_runs: 0

 build:
    needs: check
    if: ${{ needs.check.outputs.should_build == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - TARGET: arm7l-koolshare
            OS: ubuntu-latest
            GCC: arm-linux-musleabi
          - TARGET: arm7l-swrt
            OS: ubuntu-latest
            GCC: arm-linux-musleabi
          - TARGET: arm7l-opt
            OS: ubuntu-latest
            GCC: arm-linux-musleabi
          - TARGET: arm7l-etc
            OS: ubuntu-latest
            GCC: arm-linux-musleabi
          - TARGET: mipsel
            OS: ubuntu-latest
            GCC: mipsel-linux-muslsf
          - TARGET: mipsel-opt
            OS: ubuntu-latest
            GCC: mipsel-linux-muslsf
    runs-on: ${{ matrix.OS }}
    env:
      TARGET: ${{ matrix.TARGET }}
      OS: ${{ matrix.OS }}
      GCC: ${{ matrix.GCC }}
    steps:
      - uses: actions/checkout@v4
      - name: 下载编译工具链
        shell: bash
        run: |
          set -euo pipefail
          echo -e "\n\033[42;31;5;1m 开始克隆${{ needs.check.outputs.ver }}源码... \n\033[0m"
          git clone --depth 1 --branch "${{ needs.check.outputs.ver }}" https://github.com/zerotier/ZeroTierOne /opt/zt
          case $TARGET in
              mipsel)
                MUSL_URI=mipsel-linux-muslsf-cross
                ;;
              mipsel-opt)
                MUSL_URI=mipsel-linux-muslsf-cross
                ;;
              arm7l-koolshare)
                MUSL_URI=arm-linux-musleabi-cross
                ;;
              arm7l-swrt)
                MUSL_URI=arm-linux-musleabi-cross
                ;;
              arm7l-opt)
                MUSL_URI=arm-linux-musleabi-cross
                ;;
              arm7l-etc)
                MUSL_URI=arm-linux-musleabi-cross
                ;;
            esac
          echo "MUSL_URI=${MUSL_URI}" >> $GITHUB_ENV
          mkdir -p /opt/musl_gcc
          echo -e "\n\033[42;31;5;1m 开始下载musl工具链.. \n\033[0m"
          TOOLCHAIN_ARCHIVE="$MUSL_URI.tgz"
          TOOLCHAIN_URL="https://github.com/lmq8267/Toolchain/releases/download/musl-cross/${TOOLCHAIN_ARCHIVE}"
          curl -fsSL --retry 3 --retry-delay 2 "$TOOLCHAIN_URL" -o "/opt/musl_gcc/${TOOLCHAIN_ARCHIVE}"
          TOOLCHAIN_SHA256="$(curl -fsSL --retry 3 --retry-delay 2 https://api.github.com/repos/lmq8267/Toolchain/releases/tags/musl-cross | jq -r --arg name "${TOOLCHAIN_ARCHIVE}" '.assets[] | select(.name == $name) | .digest // empty' | sed 's/^sha256://')"
          [ -n "$TOOLCHAIN_SHA256" ] || { echo "未能读取 ${TOOLCHAIN_ARCHIVE} 的 sha256 digest"; exit 1; }
          echo "$TOOLCHAIN_SHA256  /opt/musl_gcc/${TOOLCHAIN_ARCHIVE}" | sha256sum -c -
          echo -e "\n\033[42;31;5;1m 开始解压musl工具链.. \n\033[0m"
          tar -xf "/opt/musl_gcc/${TOOLCHAIN_ARCHIVE}" -C /opt/musl_gcc/
          echo -e "\n\033[42;31;5;1m 查看musl-gcc信息 \n\033[0m"
          /opt/musl_gcc/$MUSL_URI/bin/${{ matrix.GCC }}-gcc -v
      -
        name: 编译zerotier
        shell: bash
        run: |
          set -euo pipefail
          cd /opt/zt
          echo -e "\n\033[42;31;5;1m 修改zerotier源码 \n\033[0m"
          sed -i 's|-fPIC -fPIE|-fPIC -fPIE -static|g' ./make-linux.mk
          sed -i '/include objects.mk/i    LDLIBS+=-latomic' ./make-linux.mk
          sed -i 's|override CFLAGS+=-march=armv5t -mfloat-abi=soft -msoft-float -mno-unaligned-access -marm|override CFLAGS+=-mfloat-abi=soft -msoft-float -mno-unaligned-access -marm|g' ./make-linux.mk
          sed -i 's|override CXXFLAGS+=-march=armv5t -mfloat-abi=soft -msoft-float -mno-unaligned-access -marm|override CXXFLAGS+=-mfloat-abi=soft -msoft-float -mno-unaligned-access -marm|g' ./make-linux.mk
          sed -i 's|override CFLAGS+=-mfloat-abi=hard -march=armv6zk -marm -mfpu=vfp -mno-unaligned-access -mtp=cp15 -mcpu=arm1176jzf-s|override CFLAGS+=-mfloat-abi=soft -msoft-float -mno-unaligned-access -marm|g' ./make-linux.mk
          sed -i 's|override CXXFLAGS+=-mfloat-abi=hard -march=armv6zk -marm -mfpu=vfp -fexceptions -mno-unaligned-access -mtp=cp15 -mcpu=arm1176jzf-s|override CXXFLAGS+=-mfloat-abi=soft -msoft-float -mno-unaligned-access -marm|g' ./make-linux.mk

          if [[ $TARGET == mipsel ]] ; then
            echo -e "\n\033[42;31;5;1m 修改mipsel架构密钥存储路径 \n\033[0m"
            sed -i 's|/var/lib/zerotier-one|/etc/storage/zerotier-one|g' ./osdep/OSUtils.cpp
          fi

          if [[ $TARGET == mipsel-opt ]] ; then
            echo -e "\n\033[42;31;5;1m 修改mipsel架构Entware密钥存储路径 \n\033[0m"
            sed -i 's|/var/lib/zerotier-one|/opt/var/lib/zerotier-one|g' ./osdep/OSUtils.cpp
          fi

          if [[ $TARGET == arm7l-opt ]] ; then
            echo -e "\n\033[42;31;5;1m 修改arm7l架构Entware密钥存储路径 \n\033[0m"
            sed -i 's|/var/lib/zerotier-one|/opt/var/lib/zerotier-one|g' ./osdep/OSUtils.cpp
          fi

          if [[ $TARGET == arm7l-etc ]] ; then
            echo -e "\n\033[42;31;5;1m 修改arm7l架构Entware密钥存储路径 \n\033[0m"
            sed -i 's|/var/lib/zerotier-one|/etc/zerotier-one|g' ./osdep/OSUtils.cpp
          fi

          if [[ $TARGET == arm7l-koolshare ]] ; then
            echo -e "\n\033[42;31;5;1m 修改arm7l架构koolshare密钥存储路径 \n\033[0m"
            sed -i 's|/var/lib/zerotier-one|/koolshare/configs/zerotier-one|g' ./osdep/OSUtils.cpp
          fi

          if [[ $TARGET == arm7l-swrt ]] ; then
            echo -e "\n\033[42;31;5;1m 修改arm7l架构swrt密钥存储路径 \n\033[0m"
            sed -i 's|/var/lib/zerotier-one|/jffs/softcenter/etc/zerotier-one|g' ./osdep/OSUtils.cpp
          fi

          grep /zerotier-one ./osdep/OSUtils.cpp
          echo -e "\n\033[42;31;5;1m 应用旧内核TAP兼容补丁（set MAC/MTU before UP） \n\033[0m"
          python3 "$GITHUB_WORKSPACE/tools/patch-zerotier-linux-tap.py" ./osdep/LinuxEthernetTap.cpp

          echo -e "\n\033[42;31;5;1m 设置编译环境变量 \n\033[0m"
          sudo timedatectl set-timezone "Asia/Shanghai"
          export PATH=$PATH:/opt/musl_gcc/${{ env.MUSL_URI }}/bin
          export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/opt/musl_gcc/${{ env.MUSL_URI }}/${{ matrix.GCC }}/lib
          export CC=${{ matrix.GCC }}-gcc
          export CXX=${{ matrix.GCC }}-g++
          export AR=${{ matrix.GCC }}-ar
          export LD=${{ matrix.GCC }}-ld
          export RANLIB=${{ matrix.GCC }}-ranlib
          export STRIP=${{ matrix.GCC }}-strip
          export CFLAGS="-I /opt/musl_gcc/${{ env.MUSL_URI }}/${{ matrix.GCC }}/include -L /opt/musl_gcc/${{ env.MUSL_URI }}/${{ matrix.GCC }}/lib -Wl,--gc-sections -Os -ffunction-sections -fdata-sections -D_GNU_SOURCE -D_BSD_SOURCE -fPIE"
          export CXXFLAGS="$CFLAGS"
          export TARGET_CFLAGS=" -DNDEBUG -DBOOST_NO_FENV_H -DBOOST_DISABLE_ASSERTS"
          echo -e "\n\033[42;31;5;1m 开始编译... \n\033[0m"

          CC=${{ matrix.GCC }}-gcc \
          STRIP=${{ matrix.GCC }}-strip \
          ZT_STATIC=1 \
          OSTYPE="Linux" \
          make

      - name: 安装 UPX
        uses: crazy-max/ghaction-upx@v3
        with:
          version: v4.2.4
          install-only: true
      -
        name: 剥离压缩
        run: |
          set -euo pipefail
          cd /opt/zt
          echo -e "\n\033[42;31;5;1m 查看二进制程序信息 \n\033[0m"
          file zerotier-one
          strings zerotier-one | grep /zerotier-one
          echo -e "\n\033[42;31;5;1m 查看原始体积 \n\033[0m"
          du -k zerotier-one
          echo -e "\n\033[42;31;5;1m 开始剥离... \n\033[0m"
          /opt/musl_gcc/${{ env.MUSL_URI }}/bin/${{ matrix.GCC }}-strip zerotier-one
          echo -e "\n\033[42;31;5;1m 查看剥离后的体积 \n\033[0m"
          du -k zerotier-one
          echo -e "\n\033[42;31;5;1m 开始upx压缩... \n\033[0m"
          upx --lzma --best zerotier-one
          echo -e "\n\033[42;31;5;1m 查看压缩后的体积 \n\033[0m"
          du -k zerotier-one
          echo -e "\n\033[42;31;5;1m 编译时间：$(date '+%Y-%m-%d %H:%M:%S') \n\033[0m"
          echo "build_time=$(date '+%Y年%m月%d日%H:%M:%S' | jq -sRr @uri)" >> $GITHUB_ENV
      -
        name: 打包上传
        shell: bash
        run: |
         set -euo pipefail
         BASE=`pwd`
         if [[ $TARGET == mipsel ]] ; then
           mkdir -p /opt/bin/zerotier-one
           echo -e "\n\033[42;31;5;1m 打包padavan的zerotier \n\033[0m"
           cp /opt/zt/zerotier-one /opt/bin/zerotier-one/zerotier-one
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-cli
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-idtool
           cd /opt/bin
           tar czvf zerotier.tar.gz zerotier-one
           if [[ $TARGET == mipsel ]] ; then
              echo $(md5sum /opt/bin/zerotier.tar.gz | awk '{print $1;}') >tarMD5.txt
              cd /opt/bin/zerotier-one
              echo $(md5sum /opt/bin/zerotier-one/zerotier-one | awk '{print $1;}') >MD5.txt
              git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
              git config --global user.name "github-actions[bot]"
              cd $BASE
              [ ! -d "install/${{ needs.check.outputs.ver }}" ] && mkdir -p "install/${{ needs.check.outputs.ver }}"
              cp -rf /opt/bin/zerotier-one/zerotier-one install/${{ needs.check.outputs.ver }}/zerotier-one
              cp -rf /opt/bin/zerotier-one/MD5.txt install/${{ needs.check.outputs.ver }}/MD5.txt
              cp -rf /opt/bin/zerotier.tar.gz install/${{ needs.check.outputs.ver }}/zerotier.tar.gz
              cp -rf /opt/bin/tarMD5.txt install/${{ needs.check.outputs.ver }}/tarMD5.txt
              (cd install && sha256sum zero.tar.gz zerotier-one [0-9]*/zerotier-one [0-9]*/zerotier.tar.gz > SHA256SUMS)
              git add install/${{ needs.check.outputs.ver }} install/SHA256SUMS
              if ! git diff --cached --quiet; then
                git commit -m "上传时间： $(date '+%Y-%m-%d %H:%M:%S')"
                git push --quiet "https://${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
              else
                echo "没有新的 mipsel 产物需要提交"
              fi
           fi
            mkdir -p /opt/ipk
            wget -q -c https://github.com/lmq8267/ZeroTierOne/releases/download/1.12.1/zerotier_1.12.2-1_mipsel-3.4.ipk -P /opt/ipk/
            echo -e "\n\033[42;31;5;1m 开始打包Entware软件中心IPK安装包.. \n\033[0m"
            tar xzf /opt/ipk/zerotier_1.12.2-1_mipsel-3.4.ipk -C /opt/ipk/
            tar -xf /opt/ipk/data.tar.gz -C /opt/ipk/
            rm -rf /opt/ipk/opt/bin/zerotier-one /opt/ipk/data.tar.gz
            cp -rf /opt/bin/zerotier-one/zerotier-one /opt/ipk/opt/bin/zerotier-one
            cd /opt/ipk
            tar czvf data.tar.gz opt
            tar -czf zerotier_${{ needs.check.outputs.ver }}-1_mipsel-3.4.ipk data.tar.gz control.tar.gz debian-binary
         fi
         if [[ $TARGET == mipsel-opt ]] ;then
           mkdir -p /opt/bin/zerotier-one
           echo -e "\n\033[42;31;5;1m 打包mipsel-opt-zerotier \n\033[0m"
           cp /opt/zt/zerotier-one /opt/bin/zerotier-one/zerotier-one
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-cli
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-idtool
           cd /opt/bin
           tar czvf zerotier-mipsel-opt.tar.gz zerotier-one
           mkdir -p /opt/ipk
            wget -q -c https://github.com/lmq8267/ZeroTierOne/releases/download/1.12.1/zerotier_1.12.2-1_mipsel-3.4.ipk -P /opt/ipk/
            echo -e "\n\033[42;31;5;1m 开始打包Entware软件中心IPK安装包.. \n\033[0m"
            tar xzf /opt/ipk/zerotier_1.12.2-1_mipsel-3.4.ipk -C /opt/ipk/
            tar -xf /opt/ipk/data.tar.gz -C /opt/ipk/
            rm -rf /opt/ipk/opt/bin/zerotier-one /opt/ipk/data.tar.gz
            cp -rf /opt/bin/zerotier-one/zerotier-one /opt/ipk/opt/bin/zerotier-one
            cd /opt/ipk
            tar czvf data.tar.gz opt
            tar -czf zerotier-opt_${{ needs.check.outputs.ver }}-1_mipsel-3.4.ipk data.tar.gz control.tar.gz debian-binary
          fi
         if [[ $TARGET == arm7l-opt ]] ;then
           mkdir -p /opt/bin/zerotier-one
           echo -e "\n\033[42;31;5;1m 打包arm7l-opt-zerotier \n\033[0m"
           cp /opt/zt/zerotier-one /opt/bin/zerotier-one/zerotier-one
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-cli
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-idtool
           cd /opt/bin
           tar czvf zerotier-arm7l-opt.tar.gz zerotier-one

           ls /opt/bin/zerotier-arm7l-opt.tar.gz
         fi
         if [[ $TARGET == arm7l-etc ]] ;then
           mkdir -p /opt/bin/zerotier-one
           echo -e "\n\033[42;31;5;1m 打包arm7l-etc-zerotier \n\033[0m"
           cp /opt/zt/zerotier-one /opt/bin/zerotier-one/zerotier-one
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-cli
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-idtool
           cd /opt/bin
           tar czvf zerotier-arm7l-etc.tar.gz zerotier-one
         fi
         if [[ $TARGET == arm7l-swrt ]] ;then
           mkdir -p /opt/bin/zerotier-one
           echo -e "\n\033[42;31;5;1m 打包arm7l-swrt-zerotier \n\033[0m"
           cp /opt/zt/zerotier-one /opt/bin/zerotier-one/zerotier-one
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-cli
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-idtool
           cd /opt/bin
           tar czvf zerotier-arm7l-swrt.tar.gz zerotier-one
         fi
         if [[ $TARGET == arm7l-koolshare ]] ;then
           mkdir -p /opt/bin/zerotier-one
           echo -e "\n\033[42;31;5;1m 打包arm7l-koolshare-zerotier \n\033[0m"
           cp /opt/zt/zerotier-one /opt/bin/zerotier-one/zerotier-one
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-cli
           ln -sf /opt/bin/zerotier-one/zerotier-one /opt/bin/zerotier-one/zerotier-idtool
           cd /opt/bin
           tar czvf zerotier-arm7l-koolshare.tar.gz zerotier-one
         fi

         find /opt/bin -maxdepth 3 -type f -print || true
         find /opt/ipk -maxdepth 2 -type f -print || true
      #-
        #name: 打包梅林离线安装包
        #shell: bash
        #run: |
         #if [[ $TARGET == arm7l-koolshare ]] ; then
         #mkdir -p /opt/koolshare
         #wget -q -c https://github.com/lmq8267/ZeroTierOne/releases/download/koolshare/zerotier1.12.2.tar.gz -P /opt/koolshare/
         #echo -e "\n\033[42;31;5;1m 开始打包koolshare版.. \n\033[0m"
         #tar -xf /opt/koolshare/zerotier1.12.2.tar.gz -C /opt/koolshare/
         #rm -rf /opt/koolshare/zerotier/bin/zerotier-one
         #cp -rf /opt/zt/zerotier-one /opt/koolshare/zerotier/bin/zerotier-one
         #sed -i 's|安装主程序zerotier-one_1.12.2|安装主程序zerotier-one_${{ needs.check.outputs.ver }}|g' /opt/koolshare/zerotier/install.sh
         #sed -i 's|1.12.2|${{ needs.check.outputs.ver }}|g' /opt/koolshare/zerotier/webs/Module_zerotier.asp
         #cat /opt/koolshare/zerotier/webs/Module_zerotier.asp | grep ZeroTier
         #cat /opt/koolshare/zerotier/install.sh | grep zerotier-one_
         #cd /opt/koolshare
         #tar czvf zerotier-arm7l-koolshare.tar.gz zerotier
         #fi

         #if [[ $TARGET == arm7l-swrt ]] ; then
         #mkdir -p /opt/swrt
         #wget -q -c https://github.com/lmq8267/ZeroTierOne/releases/download/swrt/zerotier1.12.2.tar.gz -P /opt/swrt/
         #echo -e "\n\033[42;31;5;1m 开始打包swrt版.. \n\033[0m"
         #tar -xf /opt/swrt/zerotier1.12.2.tar.gz -C /opt/swrt/
         #rm -rf /opt/swrt/zerotier/bin/zerotier-one
         #cp -rf /opt/zt/zerotier-one /opt/swrt/zerotier/bin/zerotier-one
         #sed -i 's|version=1.12.2|version=${{ needs.check.outputs.ver }}|g' /opt/swrt/zerotier/install.sh
         #cat /opt/swrt/zerotier/install.sh | grep version=
         #cd /opt/swrt
         #tar czvf zerotier-${{ needs.check.outputs.ver }}.tar.gz zerotier
         #fi
      #-
        #name: 发布prerelease
        #uses: softprops/action-gh-release@v1
        #with:
          #token: ${{ secrets.c8 }}
          #body: |
           #> ### 编译时间：${{ env.build_time }}
           #- **密钥存储位置已改为：/jffs/softcenter/etc/zerotier-one**
           #- **这是适用于arm7l架构P大的swrt梅林固件软件中心的zerotier二进制文件**
           #- **下载直接解压出zerotier-one 替换原有的zerotier-one即可**

           #**[官方更新说明](https://github.com/zerotier/ZeroTierOne/releases)**
          #prerelease: true
          #tag_name: swrt
          #files: /opt/bin/zerotier-arm7l-swrt.tar.gz
      #-
        #name: 发布prerelease
        #uses: softprops/action-gh-release@v1
        #with:
          #token: ${{ secrets.c8 }}
          #body: |
           #> ### 编译时间：${{ env.build_time }}
           #- **密钥存储位置已改为：/koolshare/configs/zerotier-one**
           #- **这是适用于arm7l架构koolshare的梅林固件384/386软件中心的离线安装包**

           #**[官方更新说明](https://github.com/zerotier/ZeroTierOne/releases)**
          #prerelease: true
          #tag_name: koolshare
          #files: /opt/koolshare/zerotier-${{ needs.check.outputs.ver }}.tar.gz
      -
        name: 发布release
        if: matrix.TARGET != 'mipsel' && matrix.TARGET != 'mipsel-opt'
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
           > ### ![](https://img.shields.io/badge/%E7%BC%96%E8%AF%91%E6%97%B6%E9%97%B4-${{ env.build_time }}-8267?logo=github&labelColor=%E9%A1%BB)![](https://img.shields.io/github/downloads/${{ github.repository }}/${{ needs.check.outputs.ver }}/total?label=%E4%B8%8B%E8%BD%BD%E6%AC%A1%E6%95%B0&logo=github)

           **文件说明：**

           - **zerotier.tar.gz**    mipsel架构padavan固件压缩包 密钥路径 **/etc/storage/zerotier-one**
           - **tarMD5.txt** 为 **zerotier.tar.gz** 压缩包的MD5校验值
           - **zerotier-one**  mipsel架构padavan固件单独二进制文件 密钥路径 **/etc/storage/zerotier-one**
           - **MD5.txt**  为 **zerotier-one** 单独二进制文件的MD5校验值
           - **zerotier-arm7l-opt.tar.gz**   arm7l架构密钥路径 **/opt/var/lib/zerotier-one**
           - **zerotier-arm7l-etc.tar.gz**   arm7l架构密钥路径 **/etc/zerotier-one**
           - **zerotier-mipsel-opt.tar.gz**   mipsel架构密钥路径 **/opt/var/lib/zerotier-one**
           - **zerotier-arm7l-swrt.tar.gz**   arm7l架构P大的swrt的改版梅林固件密钥路径 **/jffs/softcenter/etc/zerotier-one**
           - **zerotier-arm7l-koolshare.tar.gz**  arm7l架构koolshare的改版梅林固件密钥路径  **/koolshare/configs/zerotier-one**
           - **zerotier_${{ needs.check.outputs.ver }}-1_mipsel-3.4.ipk**   Entware软件包密钥路径 **/etc/storage/zerotier-one**
           - **zerotier-opt_${{ needs.check.outputs.ver }}-1_mipsel-3.4.ipk**   Entware软件包密钥路径 **/opt/var/lib/zerotier-one**

           **[官方更新说明](https://github.com/zerotier/ZeroTierOne/releases)**
          tag_name: ${{ needs.check.outputs.ver }}
          fail_on_unmatched_files: true
          files: |
            /opt/bin/*.tar.gz

      -
        name: 发布release
        if: matrix.TARGET == 'mipsel'
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
           > ### ![](https://img.shields.io/badge/%E7%BC%96%E8%AF%91%E6%97%B6%E9%97%B4-${{ env.build_time }}-8267?logo=github&labelColor=%E9%A1%BB)![](https://img.shields.io/github/downloads/${{ github.repository }}/${{ needs.check.outputs.ver }}/total?label=%E4%B8%8B%E8%BD%BD%E6%AC%A1%E6%95%B0&logo=github)

           **文件说明：**

           - **zerotier.tar.gz**    mipsel架构padavan固件压缩包 密钥路径 **/etc/storage/zerotier-one**
           - **tarMD5.txt** 为 **zerotier.tar.gz** 压缩包的MD5校验值
           - **zerotier-one**  mipsel架构padavan固件单独二进制文件 密钥路径 **/etc/storage/zerotier-one**
           - **MD5.txt**  为 **zerotier-one** 单独二进制文件的MD5校验值
           - **zerotier-arm7l-opt.tar.gz**   arm7l架构密钥路径 **/opt/var/lib/zerotier-one**
           - **zerotier-arm7l-etc.tar.gz**   arm7l架构密钥路径 **/etc/zerotier-one**
           - **zerotier-mipsel-opt.tar.gz**   mipsel架构密钥路径 **/opt/var/lib/zerotier-one**
           - **zerotier-arm7l-swrt.tar.gz**   arm7l架构P大的swrt的改版梅林固件密钥路径 **/jffs/softcenter/etc/zerotier-one**
           - **zerotier-arm7l-koolshare.tar.gz**  arm7l架构koolshare的改版梅林固件密钥路径  **/koolshare/configs/zerotier-one**
           - **zerotier_${{ needs.check.outputs.ver }}-1_mipsel-3.4.ipk**   Entware软件包密钥路径 **/etc/storage/zerotier-one**
           - **zerotier-opt_${{ needs.check.outputs.ver }}-1_mipsel-3.4.ipk**   Entware软件包密钥路径 **/opt/var/lib/zerotier-one**

           **[官方更新说明](https://github.com/zerotier/ZeroTierOne/releases)**
          tag_name: ${{ needs.check.outputs.ver }}
          fail_on_unmatched_files: true
          files: |
            /opt/bin/*.tar.gz
            /opt/bin/tarMD5.txt
            /opt/bin/zerotier-one/zerotier-one
            /opt/bin/zerotier-one/MD5.txt
            /opt/ipk/*.ipk

      -
        name: 发布release
        if: matrix.TARGET == 'mipsel-opt'
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
           > ### ![](https://img.shields.io/badge/%E7%BC%96%E8%AF%91%E6%97%B6%E9%97%B4-${{ env.build_time }}-8267?logo=github&labelColor=%E9%A1%BB)![](https://img.shields.io/github/downloads/${{ github.repository }}/${{ needs.check.outputs.ver }}/total?label=%E4%B8%8B%E8%BD%BD%E6%AC%A1%E6%95%B0&logo=github)

           **文件说明：**

           - **zerotier-mipsel-opt.tar.gz**   mipsel架构密钥路径 **/opt/var/lib/zerotier-one**
           - **zerotier-opt_${{ needs.check.outputs.ver }}-1_mipsel-3.4.ipk**   Entware软件包密钥路径 **/opt/var/lib/zerotier-one**

           **[官方更新说明](https://github.com/zerotier/ZeroTierOne/releases)**
          tag_name: ${{ needs.check.outputs.ver }}
          fail_on_unmatched_files: true
          files: |
            /opt/bin/*.tar.gz
            /opt/ipk/*.ipk

      #- name: Push changes
        #uses: ad-m/github-push-action@master
        #with:
          #force_with_lease: true

 keepalive-workflow:
    # github超过60天未提交将自动停止工作流
    name: 工作流保活
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
    steps:
      - uses: actions/checkout@v4
      #- uses: gautamkrishnar/keepalive-workflow@v2
        #with:
          #use_api: true
      - name: 检查上次提交日期
        id: check_commit_date
        run: |
          # 获取上次提交日期（格式：YYYY-MM-DD）
          LAST_COMMIT_DATE=$(git log -1 --format=%cd --date=short)
          echo "上次提交日期: $LAST_COMMIT_DATE"
          # 计算当前日期和上次提交日期的差值（单位：天）
          LAST_COMMIT_TIMESTAMP=$(date -d "$LAST_COMMIT_DATE" +%s)
          CURRENT_TIMESTAMP=$(date +%s)
          DIFF_DAYS=$(( (CURRENT_TIMESTAMP - LAST_COMMIT_TIMESTAMP) / 86400 ))
          echo "距离上次提交以来的天数: $DIFF_DAYS"
          echo "diff_days=$DIFF_DAYS" >> $GITHUB_ENV
      - name: 执行空提交
        if: ${{ env.diff_days > 50 }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # 创建空提交
          git commit --allow-empty -m "自动空提交，保持仓库活跃"
          git push
